#
# ~/.bash_profile
#
#EMACS STUFF
which emacs > /dev/null 2>&1
if [ "0" -eq "$?" ]; then
	WHO=`whoami`
	RET=`pgrep -flu ${WHO} "emacs --daemon"`
	if [ "${RET}" = "" ]; then
		emacs --daemon > /dev/null 2>&1
	fi
fi

#OTHER STUFF
[[ -f ~/.bashrc ]] && . ~/.bashrc
