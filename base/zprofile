#!/usr/bin/env zsh

# Attempt to Update Configs
if [ "$(ps ux | grep zsh | wc -l)" -le "2" ]; then
	echo -n "Updating... "
	"$(dirname "$(readlink -f "$HOME/.zprofile")")/update-git"
	echo "Done!"
fi
